---
description: 학생 활동지(Worksheet)를 생성한다.
argument-hint: <활동/수업 목표>
---

다음 내용으로 워크시트를 만든다: $ARGUMENTS

`worksheet` subagent를 사용해서 `.claude/templates/html/worksheet.html` 구조를 기반으로 워크시트를 생성한다.

## Output

- Student Worksheet
- Teacher Note
