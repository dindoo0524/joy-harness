---
description: 완성된 콘텐츠를 현장에서 바로 쓸 수 있는 형태로 published/ 아래에 정리한다.
argument-hint: <완성된 콘텐츠 파일 경로>
---

$ARGUMENTS 파일을 실제 수업에서 바로 쓸 최종본으로 취급하고 `published/YYYY-MM-DD-<slug>/` 아래로 정리해서 복사한다.

- HTML은 그대로 복사한다 (외부 의존성 없는 단일 파일인지 다시 확인한다).
- 함께 필요한 이미지/자료가 있으면 같은 폴더에 포함한다.
- 정리 후 최종 경로를 알려준다.
