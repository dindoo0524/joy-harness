# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty (no commits, no source files). There is no build system, test suite, or code architecture to document yet.

When code is added to this repository, regenerate this file (`/init`) so it reflects the actual commands (build/lint/test) and architecture at that point.
